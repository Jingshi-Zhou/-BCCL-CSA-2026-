# 评分细则（基于数据集）



---

## 1 数据集结构说明

每条样本包含以下字段：

| 字段 | 类型 | 说明 |
|------|------|------|
| `sentiment.label` | 字符串 | 情感极性：`positive` / `negative` / `neutral` |
| `sentiment.point` | 浮点数 | 情感强度，范围 [-0.8, 0.9]，0 为中性 |
| `emotion.angry` | 浮点数 | 愤怒概率，7项情感概率之和 = 1.0 |
| `emotion.disgusted` | 浮点数 | 厌恶概率 |
| `emotion.fearful` | 浮点数 | 恐惧概率 |
| `emotion.happy` | 浮点数 | 愉快概率 |
| `emotion.sad` | 浮点数 | 悲伤概率 |
| `emotion.surprised` | 浮点数 | 惊讶概率 |
| `emotion.neutral` | 浮点数 | 中性概率 |

**数据集规模：**

| 分割 | 样本数（中文） | 样本数（英文） |
|------|---------------|---------------|
| 训练集 | 1,562 | 1,562 |
| 验证集 | 263 | 263 |
| 测试集（公开） | 782 | 782 |
| 测试集（配对） | 782 对（含中英文本，无标签） | — |

> **重要注意**：中英文对同一文本的标注**不总是相同**（训练集中英标签一致率约 84.6%），反映了不同语言读者的真实情感感知差异。

---

## 2 训练集标签分布

| 类别 | 中文数量 | 英文数量 |
|------|----------|----------|
| `neutral` | 1,086（69.5%） | 971（62.2%） |
| `positive` | 243（15.6%） | 347（22.2%） |
| `negative` | 233（14.9%） | 244（15.6%） |

> 类别严重不均衡（中性占多数），因此以 **Macro-F1** 而非 Accuracy 作为主排名指标。

---

## 3 公共指标定义

### 3.1 Macro-F1（主指标）

对 3 个类别（positive / negative / neutral）分别计算 F1，再取算术平均：

```
F1_c = 2 × Precision_c × Recall_c / (Precision_c + Recall_c)

Macro-F1 = (F1_positive + F1_negative + F1_neutral) / 3
```

> 不受类别不均衡影响，中性类样本多不会拉高分数。

### 3.2 Accuracy

```
Accuracy = 预测正确的样本数 / 总样本数
```

作为辅助指标，不单独排名。

### 3.3 EMOS（情感分布相似度）

EMOS 衡量预测的 7 维情感概率向量与真值的接近程度，由三个子指标合成：

| 子指标 | 输入 | 含义 |
|--------|------|------|
| **VSS**（值相似度） | 预测向量 vs 真值向量 | 情感强度数值的接近程度（如余弦相似度） |
| **LMR**（主情感匹配率） | argmax(预测) vs argmax(真值) | 最强情感类型是否预测正确 |
| **EDR**（分布一致性） | 预测分布 vs 真值分布 | 整体情感分布形状的相似度 |

```
EMOS = (VSS + LMR + EDR) / 3

所有子指标归一化到 [0, 1]，EMOS 最终值也在 [0, 1]
```

---

## 4 子任务一：中文情感分析

**输入**：中文文本（`text` 字段）
**预测目标**：`sentiment.label` + `emotion` 7维分布
**对比真值**：中文标注（`train_zh.jsonl` / `dev_zh.jsonl`）

### 评分公式

```
Score_ZH = Macro-F1(label) × 0.5
           + EMOS(pred_emotion, true_emotion_zh) × 0.3
           + Accuracy(label) × 0.2
```

---

## 5 子任务二：英文情感分析

**输入**：英文文本（`text` 字段）
**预测目标**：`sentiment.label` + `emotion` 7维分布
**对比真值**：英文标注（`train_en.jsonl` / `dev_en.jsonl`）

### 评分公式

```
Score_EN = Macro-F1(label) × 0.5
           + EMOS(pred_emotion, true_emotion_en) × 0.3
           + Accuracy(label) × 0.2
```

> 与子任务一结构相同，但使用英文独立标注作为真值（英文真值与中文真值约 84.6% 一致）。

---

## 6 子任务三：中英一致性评估

**输入**：中英配对文本（`test_pairs_public.jsonl`，含 `zh.text` 和 `en.text`）
**预测目标**：对同一文本的中英两版本分别输出预测
**目标**：中英预测互相一致，且不奖励"中英同错"

### 三项子指标

**① LabelConsistency（权重 0.4）**

```
LabelConsistency = 中英标签预测相同的样本数 / 总样本数

即：count(pred_label_zh == pred_label_en) / N
```

**② EMOS互比（权重 0.3）**

```
EMOS_cross = EMOS(pred_emotion_zh, pred_emotion_en)

衡量系统在同一文本的中英预测在情感空间的相似度
分数越高 = 中英情感输出越一致
```

**③ CorrectnessAnchor（权重 0.3）**

```
CorrectnessAnchor = 满足"中英一致 且 至少一个语言预测正确"的样本数 / N
```

> 防止"中英同错"陷阱：系统若对所有样本都预测 neutral，LabelConsistency 会很高，但 CorrectnessAnchor 会揭示其错误。

### 综合得分

```
Score_Pair = LabelConsistency × 0.4
           + EMOS_cross × 0.3
           + CorrectnessAnchor × 0.3
```

---

## §7 EMOS 五档量表

> 来源：Zhou et al., 2026。仅供人工参考，**不参与自动排名**。

| 档位 | EMOS 区间 | 描述 |
|------|-----------|------|
| 优秀 | 0.85 ~ 1.00 | 情感输出高度一致，接近人工标注 |
| 良好 | 0.70 ~ 0.84 | 主要情感方向正确，细节略有偏差 |
| 一般 | 0.55 ~ 0.69 | 情感强度或分布存在明显差异 |
| 较差 | 0.40 ~ 0.54 | 仅部分情感信号被捕捉 |
| 很差 | 0.00 ~ 0.39 | 情感输出与真值差异显著 |

---

## 8 难度子集报告

**排名依据**：全量测试集计算最终得分。

**强制报告**（不影响排名，供分析用）：

| 子集 | 定义参考 |
|------|----------|
| Easy | `sentiment.point` 绝对值 > 0.5（情感极性明显） |
| Medium | `sentiment.point` 绝对值在 0.2 ~ 0.5 之间 |
| Hard | `sentiment.point` 绝对值 < 0.2（情感模糊），或中英标注标签不一致的样本 |

---

## 9 补充诊断指标

不参与排名，仅供系统分析参考：

| 指标 | 针对字段 | 用途 |
|------|----------|------|
| MAE | `sentiment.point` | 情感强度数值预测误差（范围 [-0.8, 0.9]） |
| Pearson 相关系数 | `sentiment.point` | 预测强度与真值强度的线性相关性 |
| Transfer Gap | — | 子任务一 Macro-F1 − 子任务二 Macro-F1，衡量中→英迁移损耗 |

---

## 10 提交格式参考

**子任务一/二**（每条样本需预测）：
```json
{
  "id": 2,
  "sentiment": {"label": "neutral", "point": 0.1},
  "emotion": {"angry": 0.05, "disgusted": 0.05, "fearful": 0.1,
              "happy": 0.2, "sad": 0.2, "surprised": 0.1, "neutral": 0.3}
}
```

**子任务三**（配对输出）：
```json
{
  "id": 2,
  "zh": {"sentiment": {"label": "neutral"}, "emotion": {"neutral": 0.5, ...}},
  "en": {"sentiment": {"label": "neutral"}, "emotion": {"neutral": 0.45, ...}}
}
```

---

## 11 适用范围与模型规模说明

本评分细则的计算公式保持不变。为统一赛道执行口径，补充如下：

- **Track-A（不微调）**：不限制模型参数规模。
- **Track-B（微调）**：基础模型参数规模不超过 **7B**。
- LoRA/Adapter 等参数高效微调方法在 Track-B 中允许，但仍按底座模型官方标称参数规模判定。
- 参数规模判定不以量化后模型体积或显存占用为依据。
- 参赛队须在技术报告中披露模型规模与训练策略；组委会保留复核权。

> 说明：本澄清仅补充赛道适用口径，不改变已公布的评分指标与加权公式。

## 12 参考文献

1. **SPAF**：源论文（具体引用信息待补充）
2. **EMOS**：Zhou et al., 2026 — *Emotion Model Output Similarity* 框架与五档量表

---

*如需调整权重或增加子任务，请联系赛题负责人。*
